name: englishpro_backend
description: EnglishPro Backend API
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.9.2

dependencies:
  # Server Framework
  shelf: ^1.4.0
  shelf_router: ^1.1.0

  # Database
  postgres: ^3.0.0

  # Authentication
  dart_jsonwebtoken: ^2.12.0
  bcrypt: ^1.1.3

  # CORS
  shelf_cors_headers: ^0.1.5

  # Utilities
  dotenv: ^4.2.0
  logging: ^1.3.0
  intl: ^0.19.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
